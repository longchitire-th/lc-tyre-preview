'use client';
import { useEffect, useMemo, useState } from 'react';
import { ArrowUpRight, Search, Phone, MessageCircle, ShieldCheck, Truck, Wrench, Zap, ArrowRight, ChevronLeft, ChevronRight, Play, Pause, X, Send, MapPin, Menu, ShoppingBag, User, Settings2 } from 'lucide-react';
import { Tabs, TabsList, TabsTrigger, TabsContent } from '@/components/ui/tabs';
import { Dialog, DialogContent, DialogTitle, DialogDescription } from '@/components/ui/dialog';
import { Select, SelectTrigger, SelectValue, SelectContent, SelectItem } from '@/components/ui/select';
type Product={id:number;name:string;url:string;brand:string;model:string;size:string;year:string;price:number;regularPrice:number;stock:boolean;image:string;runflat:boolean};
const line='https://line.me/R/ti/p/@lcgroup';
const money=(n:number)=>new Intl.NumberFormat('th-TH').format(n);
const normalize=(value:string)=>{const s=value.toUpperCase().replace(/[\s/R-]/g,'');return /^\d{7}$/.test(s)?`${s.slice(0,3)}/${s.slice(3,5)}R${s.slice(5)}`:null};
const articles=[{category:'BUYING GUIDE',title:'205/55R16 เริ่มเลือกยางอย่างไร',text:'เริ่มจากตรวจขนาดยาง ดัชนีรับน้ำหนัก และพิกัดความเร็วตามคู่มือรถ จากนั้นเทียบรุ่น ราคา และปีผลิต อย่าใช้ขนาดยางเพียงอย่างเดียวตัดสินว่าเหมาะกับรถทุกคัน',image:'/assets/tire-4518.png'}, {category:'EV & TECHNOLOGY',title:'ก่อนเลือกยางให้รถ EV ต้องรู้อะไร',text:'ตรวจข้อกำหนดผู้ผลิตรถและรุ่นย่อยให้ตรงกันก่อนเลือกยาง หากยังไม่มีข้อมูลรองรับ ไม่ควรสรุปว่ายางรุ่นใดเหมาะกับรถจากชื่อรุ่นหรือราคาล้วน ๆ ทีม LC Tyre พร้อมช่วยตรวจสอบก่อนซื้อ',image:'/assets/tire-3237.png'}, {category:'TIRE CARE',title:'เปลี่ยนยางครั้งต่อไป วางแผนให้ครบ',text:'ตรวจสภาพยางเดิมและข้อมูลในคู่มือรถ เตรียมขนาดยางและรุ่นรถเพื่อสอบถามสินค้า พร้อมตรวจค่าใช้จ่ายติดตั้งและบริการเพิ่มเติมก่อนยืนยันนัดหมาย',image:'/assets/tire-3251.png'}];
function Choice({value,onChange,items,label}:{value:string;onChange:(v:string)=>void;items:string[];label:string}){return <Select value={value} onValueChange={v=>v&&onChange(v)}><SelectTrigger aria-label={label} className="choice"><SelectValue/></SelectTrigger><SelectContent>{items.map(v=><SelectItem key={v} value={v}>{v}</SelectItem>)}</SelectContent></Select>}
export default function Home(){
 const [products,setProducts]=useState<Product[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');
 const [size,setSize]=useState('205/55R16'),[searchSize,setSearchSize]=useState(''),[brand,setBrand]=useState('ทุกแบรนด์'),[sort,setSort]=useState('แนะนำ'),[feature,setFeature]=useState('ทั้งหมด'),[searchError,setSearchError]=useState('');
 const [detail,setDetail]=useState<Product|null>(null),[article,setArticle]=useState<number|null>(null),[modal,setModal]=useState(''),[menu,setMenu]=useState(false);
 const [slide,setSlide]=useState(0),[auto,setAuto]=useState(true),[touch,setTouch]=useState<number|null>(null);
 const [chat,setChat]=useState(false),[message,setMessage]=useState(''),[history,setHistory]=useState([{role:'bot',text:'สวัสดีค่ะ ลองพิมพ์ขนาดยาง เช่น 205/55R16 ได้เลยค่ะ นี่เป็นแชตสาธิตจากรายการตัวอย่าง ยังไม่ได้เชื่อม AI หรือสต็อกสด'}]);
 const [campaignPrice,setCampaignPrice]=useState('4,990'),[branch,setBranch]=useState('สาขาเจริญพัฒนา'),[alignment,setAlignment]=useState('ไม่เพิ่มบริการ'),[bookingDate,setBookingDate]=useState(''),[bookingMessage,setBookingMessage]=useState(''),[vehicleMessage,setVehicleMessage]=useState(''),[shareStatus,setShareStatus]=useState('');
 useEffect(()=>{fetch('/catalog.json').then(r=>{if(!r.ok)throw Error();return r.json()}).then(d=>{const data=d as {products:Product[]};if(!Array.isArray(data.products))throw Error();setProducts(data.products)}).catch(()=>setError('โหลดรายการตัวอย่างไม่สำเร็จ กรุณาโหลดหน้าใหม่')).finally(()=>setLoading(false))},[]);
 useEffect(()=>{if(!auto||window.matchMedia('(prefers-reduced-motion: reduce)').matches)return;const id=setInterval(()=>setSlide(s=>(s+1)%3),6500);return()=>clearInterval(id)},[auto]);
 const brands=['ทุกแบรนด์',...new Set(products.map(p=>p.brand))];
 const results=useMemo(()=>{const list=products.filter(p=>(!searchSize||p.size===searchSize)&&(brand==='ทุกแบรนด์'||p.brand===brand)&&(feature!=='Run Flat'||p.runflat));return list.sort((a,b)=>sort==='ราคาต่ำ → สูง'?a.price-b.price:sort==='ราคาสูง → ต่ำ'?b.price-a.price:Number(b.year)-Number(a.year)||b.id-a.id)},[products,searchSize,brand,sort,feature]);
 function search(e?:React.FormEvent){e?.preventDefault();const s=normalize(size);if(!s){setSearchError('กรอกขนาดยาง เช่น 205/55R16, 205 55 16 หรือ 2055516');return}setSearchError('');setSearchSize(s);document.getElementById('products')?.scrollIntoView({behavior:'smooth'})}
 function send(text:string){if(!text.trim())return;let response='ไม่พบข้อมูลที่ยืนยันได้ในชุดสาธิต กรุณาติดต่อเจ้าหน้าที่ โทร 098-579-5449 หรือ LINE @lcgroup ค่ะ';const n=normalize(text);if(/เจ้าหน้าที่|พนักงาน|คนขาย|โทร|ติดต่อร้าน|คุยกับคน/.test(text))response='ติดต่อเจ้าหน้าที่ LC Tyre ได้ที่ 098-579-5449 หรือ LINE @lcgroup กดปุ่มด้านล่างได้เลยค่ะ';else if(n){const ps=products.filter(p=>p.size===n).sort((a,b)=>a.price-b.price).slice(0,3);response=ps.length?`พบในชุดตัวอย่าง ${n}: `+ps.map(p=>`${p.brand} ${p.model} ปี ${p.year} ฿${money(p.price)}/เส้น`).join(' • ')+' — เรียงตามราคาเท่านั้น กรุณายืนยันราคา สต็อก และความเหมาะสมกับรถกับเจ้าหน้าที่ก่อนซื้อค่ะ':'ไม่พบข้อมูลสินค้าในชุดตัวอย่างนี้ ไม่ได้หมายความว่าหน้าร้านไม่มีสินค้า กรุณาสอบถามเจ้าหน้าที่ค่ะ'}else if(/รถ|Toyota|Corolla|Honda/i.test(text))response='ยังไม่มีฐานข้อมูลขนาดยางตามรถที่ตรวจสอบแล้วในตัวอย่างนี้ จึงไม่ระบุขนาดยางให้โดยคาดเดา กรุณาแจ้งยี่ห้อ รุ่น ปี รุ่นย่อย และขนาดจากคู่มือรถให้เจ้าหน้าที่ค่ะ';else if(/ติดตั้ง|สาขา/.test(text))response='ตามบรีฟ: สาขาเจริญพัฒนาถอด–ใส่และถ่วงล้อฟรี ส่วนจุดพาร์ตเนอร์มีค่าบริการ ต้องยืนยันเงื่อนไขกับสาขาก่อนนัดจริงค่ะ';setHistory(h=>[...h,{role:'user',text},{role:'bot',text:response}]);setMessage('')}
 const currentTitle=slide===0?< >ยางที่ใช่<br/>ให้ทุกเส้นทาง<span>เป็นของคุณ.</span></>:slide===1?<>9.9 SPECIAL<span>4 เส้น<br/>{campaignPrice} บาท</span></>:<>พร้อมออกทางไกล<span>เริ่มต้นด้วย<br/>ความมั่นใจ.</span></>;
 const installationFee=branch==='สาขาเจริญพัฒนา'?0:2000;
 return <><a className="skip" href="#finder">ข้ามไปค้นหายาง</a><div className="preview-bar">DESIGN PREVIEW <span>ตัวอย่างแยกจากเว็บจริง · ไม่มีการรับชำระเงิน</span><button onClick={()=>setModal('admin')}>ทดลองหลังบ้าน <Settings2 size={13}/></button></div><div className="topbar"><span>LONG CI GROUP — หลงฉื่อ ขึ้นชื่อเรื่องยาง</span><a href="tel:0985795449"><Phone size={14}/> 098-579-5449</a></div><header><a className="logo" href="#"><b>LC<span>TYRE</span><i>↗</i></b><small>LONG CI GROUP</small></a><nav aria-label="เมนูหลัก"><a href="#finder">ค้นหายาง</a><a href="#products">สินค้า</a><a href="#promotion">โปรโมชั่น</a><a href="#installation">จองติดตั้ง</a><a href="#news">ข่าวสาร</a></nav><div className="header-actions"><button className="icon" aria-label="บัญชีตัวอย่าง" onClick={()=>setModal('account')}><User size={19}/></button><button className="icon" aria-label="ตะกร้าตัวอย่าง" onClick={()=>setModal('cart')}><ShoppingBag size={19}/></button><a className="header-contact" href={line} target="_blank" rel="noreferrer">คุยกับเรา <ArrowUpRight size={17}/></a><button className="icon mobile-menu" aria-label="เปิดเมนู" aria-expanded={menu} onClick={()=>setMenu(!menu)}>{menu?<X/>:<Menu/>}</button></div></header>{menu&&<div className="mobile-links">{[['ค้นหายาง','finder'],['สินค้า','products'],['โปรโมชั่น','promotion'],['จองติดตั้ง / สาขา','installation'],['ข่าวสาร','news'],['ติดต่อเรา','contact']].map(([name,id])=><a key={id} onClick={()=>setMenu(false)} href={'#'+id}>{name}</a>)}</div>}
 <main><section className="hero" id="promotion" aria-label="แคมเปญ" onTouchStart={e=>setTouch(e.touches[0].clientX)} onTouchEnd={e=>{if(touch!==null&&Math.abs(e.changedTouches[0].clientX-touch)>50){setSlide((slide+(e.changedTouches[0].clientX<touch?1:2))%3);setAuto(false)}setTouch(null)}}><div className="hero-copy"><p className="eyebrow">{slide===1?'CAMPAIGN CONCEPT / โปรตัวอย่างจากบรีฟ':'YOUR NEXT JOURNEY STARTS HERE'}</p><h1>{currentTitle}</h1><p>{slide===1?'NAZZ Ultimate1EV · 195/55R15 · ยางปี 2026':slide===2?'บริการติดตั้งที่สาขาเจริญพัฒนา':'ค้นหายางตรงรุ่น เทียบราคาได้ง่าย'}<br/>{slide===1?'ยังไม่ได้ยืนยันราคาและช่วงเวลาจำหน่าย':slide===2?'ถอด–ใส่ และถ่วงล้อฟรี ตามเงื่อนไขสาขา':'พร้อมบริการติดตั้งจาก LC TYRE'}</p><a className="primary" href={slide===1?line:slide===2?'#installation':'#finder'}>{slide===1?'สอบถามโปรผ่าน LINE':slide===2?'ดูบริการติดตั้ง':'ค้นหายางของคุณ'} <ArrowUpRight size={20}/></a><div className="hero-foot"><div className="slide-controls"><button aria-label="สไลด์ก่อนหน้า" onClick={()=>{setSlide((slide+2)%3);setAuto(false)}}><ChevronLeft size={18}/></button><span>0{slide+1} / 03</span><button aria-label="สไลด์ถัดไป" onClick={()=>{setSlide((slide+1)%3);setAuto(false)}}><ChevronRight size={18}/></button><button aria-label={auto?'หยุดสไลด์อัตโนมัติ':'เล่นสไลด์อัตโนมัติ'} onClick={()=>setAuto(!auto)}>{auto?<Pause size={14}/>:<Play size={14}/>}</button></div><div className="dots">{[0,1,2].map(i=><button key={i} aria-label={'สไลด์ '+(i+1)} aria-current={slide===i?'true':undefined} className={slide===i?'active':''} onClick={()=>{setSlide(i);setAuto(false)}}/>)}</div></div></div><div className={'hero-image '+(slide===1?'campaign-art':'')}>{slide===1?<div className="campaign-type"><span>NAZZ</span><strong>9.9</strong><b>ULTIMATE1EV</b><p>195 / 55 / R15</p><small>แนวคิดแคมเปญ · ไม่มีรูปสินค้าที่ตรวจสอบแล้ว</small></div>:<img src={slide===2?'/assets/tire-3251.png':'/assets/tire-3257.png'} alt={slide===2?'Michelin Primacy SUV+ จากหน้าร้านเดิม':'Michelin Primacy 5 จากหน้าร้านเดิม'} fetchPriority="high"/>}<span className="image-tag">{slide===1?'CAMPAIGN PREVIEW':slide===2?'MICHELIN / PRIMACY SUV+':'MICHELIN / PRIMACY 5'}</span>{slide!==1&&<div className="hero-caption"><Zap size={18}/><span>ค้นหาขนาดที่ใช่<br/><b>เริ่มต้นที่ยางของคุณ</b></span></div>}</div></section>
 <section id="finder" className="finder finder-full"><div><p className="eyebrow">FIND YOUR PERFECT FIT</p><h2>หายางที่ใช่ ในไม่กี่คลิก</h2><p className="muted">เริ่มจากขนาดยางบนแก้มยาง หรือให้เราช่วยตรวจสอบ</p></div><Tabs defaultValue="size" className="finder-tabs"><TabsList><TabsTrigger value="size">ขนาดยาง</TabsTrigger><TabsTrigger value="vehicle">รุ่นรถ</TabsTrigger><TabsTrigger value="brand">แบรนด์</TabsTrigger></TabsList><TabsContent value="size"><form className="search-form" onSubmit={search}><label htmlFor="size">กรอกขนาดยาง<input id="size" value={size} onChange={e=>setSize(e.target.value)} placeholder="205/55R16" aria-describedby="search-help"/></label><button className="primary" type="submit"><Search size={20}/> ค้นหายาง</button></form><p id="search-help" className={searchError?'error':'hint'} role={searchError?'alert':undefined}>{searchError||'รองรับ 205/55R16 · 205 55 16 · 2055516'}</p></TabsContent><TabsContent value="vehicle"><form className="vehicle-form" onSubmit={e=>{e.preventDefault();setVehicleMessage('ยังไม่ได้เชื่อมฐานข้อมูล OEM ที่ตรวจสอบแล้ว กรุณาส่งข้อมูลรถให้เจ้าหน้าที่ตรวจสอบขนาดยาง')}}><div className="vehicle-grid">{['ยี่ห้อรถ','รุ่นรถ','ปี','รุ่นย่อย'].map(n=><label key={n}>{n}<input required aria-label={n} placeholder={n==='ปี'?'2023':n}/></label>)}</div><button className="primary">ตรวจสอบขนาดยาง</button></form>{vehicleMessage&&<p role="status" className="hint">{vehicleMessage} <a className="text-link" href={line}>คุยผ่าน LINE ↗</a></p>}</TabsContent><TabsContent value="brand"><div className="brand-options">{brands.filter(b=>b!=='ทุกแบรนด์').map(b=><button key={b} className="outline" onClick={()=>{setBrand(b);setSearchSize('');document.getElementById('products')?.scrollIntoView()}}>{b}</button>)}</div></TabsContent></Tabs></section>
 <div className="benefits"><span><ShieldCheck/> สินค้าจากแคตตาล็อกจริง</span><span><Truck/> จัดส่งทั่วประเทศ</span><span><Wrench/> บริการติดตั้ง</span><span><MessageCircle/> ปรึกษาผู้เชี่ยวชาญ</span></div>
 <section className="section" id="products"><div className="section-heading"><div><p className="eyebrow">CURATED FOR YOUR DRIVE</p><h2>{searchSize?'ยางขนาด '+searchSize:'ยางสำหรับทุกเส้นทาง'}</h2></div><a className="text-link" href="https://lctyre.com/" target="_blank" rel="noreferrer">ไปหน้าร้านปัจจุบัน <ArrowUpRight size={17}/></a></div><div className="catalog-toolbar"><div className="filters"><Choice value={brand} onChange={setBrand} items={brands} label="กรองแบรนด์"/><Choice value={feature} onChange={setFeature} items={['ทั้งหมด','Run Flat']} label="ประเภท"/><button className="reset" onClick={()=>{setSearchSize('');setBrand('ทุกแบรนด์');setFeature('ทั้งหมด')}}>ล้างตัวกรอง</button></div><Choice value={sort} onChange={setSort} items={['แนะนำ','ราคาต่ำ → สูง','ราคาสูง → ต่ำ']} label="เรียงสินค้า"/></div><p className="catalog-note">ข้อมูลสาธารณะ ณ 7 ก.ย. 2026 · ชุดตัวอย่าง 33 รายการ ไม่ใช่สินค้าทั้งร้าน · ราคา/สต็อกต้องยืนยันก่อนซื้อ</p>{loading?<p role="status">กำลังโหลดสินค้า…</p>:error?<p role="alert">{error}</p>:results.length===0?<div className="empty"><Search/><h3>ไม่พบสินค้าในชุดตัวอย่างนี้</h3><p>ลอง 205/55R16, 195/55R15 หรือ 265/65R17 หรือสอบถามสินค้าทั้งหมดกับเจ้าหน้าที่</p><a className="primary" href={line}>สอบถามผ่าน LINE <ArrowUpRight size={16}/></a></div>:<><div className="product-grid">{results.slice(0,8).map(p=><article className="product-card" key={p.id}><button className="product-image" onClick={()=>setDetail(p)} aria-label={'ดู '+p.name}><img loading="lazy" src={p.image} alt={p.name}/>{p.runflat&&<span className="badge">RUN FLAT</span>}</button><div className="product-info"><span className="product-brand">{p.brand}</span><h3>{p.model.replace(/^ยาง .*? (BRIDGESTONE) /,'').split(' ราค')[0]}</h3><p>{p.size} <span>· ปี {p.year}</span></p><small className="stock">{p.stock?'มีสินค้า ณ วันที่ดึงข้อมูล':'หมด ณ วันที่ดึงข้อมูล'}</small><div className="price">฿{money(p.price)} <small>/ เส้น</small></div><div className="card-actions"><button onClick={()=>setDetail(p)}>ดูรายละเอียด <ArrowUpRight size={17}/></button><button aria-label={'ถามเรื่อง '+p.model} onClick={()=>{setChat(true);send(p.size)}}><MessageCircle size={18}/></button></div></div></article>)}</div><p className="hint">แสดง {Math.min(results.length,8)} จาก {results.length} รายการในตัวกรองนี้ · ทดลองกรองแบรนด์หรือขนาดเพื่อดูรายการอื่น</p></>}</section>
 <section id="installation" className="installation"><div className="install-intro"><p className="eyebrow">MORE THAN JUST TIRES</p><h2>เลือกยางแล้ว<br/>ให้เราดูแลต่อ.</h2><p>วางแผนติดตั้งได้ในที่เดียว<br/>เห็นค่าบริการก่อนตัดสินใจ</p><span className="free">FREE<br/><b>INSTALLATION</b></span></div><div className="install-content"><div className="branch-heading"><MapPin/><div><h3>สาขาเจริญพัฒนา</h3><p>บริการถอด–ใส่ และถ่วงล้อฟรี*</p></div></div><div className="service-row"><span>ถอด–ใส่ยาง</span><strong>ฟรี</strong></div><div className="service-row"><span>ถ่วงล้อ</span><strong>ฟรี</strong></div><p className="hint">*เงื่อนไขตามบรีฟ ต้องยืนยันกับสาขาก่อนรับบริการ<br/>พาร์ตเนอร์: ถอด–ใส่–ถ่วง 2,000 บาท / ตั้งศูนย์เพิ่ม 1,000 บาท</p><div className="button-row"><button className="primary" onClick={()=>{setModal('booking');setBookingMessage('')}}>ทดลองเลือกจุดติดตั้ง <ArrowRight size={17}/></button><a className="outline" href="tel:0985795449"><Phone size={16}/> โทรหาสาขา</a></div></div></section>
 <section className="section" id="news"><div className="section-heading"><div><p className="eyebrow">THE LC TYRE JOURNAL</p><h2>รู้เรื่องยาง ก่อนออกเดินทาง</h2></div><span className="hint">ตัวอย่างรูปแบบบทความ</span></div><div className="article-grid">{articles.map((a,i)=><button className="article-card" key={a.title} onClick={()=>{setArticle(i);setShareStatus('')}}><div className="article-image"><img src={a.image} alt="ภาพยางประกอบบทความ" loading="lazy"/><span>0{i+1}</span></div><p className="eyebrow">{a.category}</p><h3>{a.title}</h3><div><span>ฉบับสาธิต · อ่าน 1 นาที</span><ArrowUpRight size={22}/></div></button>)}</div></section>
 <section className="review-section"><ShieldCheck/><div><h3>เสียงจากผู้ใช้บริการจริง</h3><p>พื้นที่สำหรับ Google Reviews — จะแสดงคะแนนและรีวิวเมื่อเชื่อมแหล่งข้อมูลจริงแล้ว</p></div></section><section id="contact" className="contact"><div><p className="eyebrow">LET’S FIND YOUR NEXT SET</p><h2>ไม่แน่ใจว่าควรเลือกยางอะไร?</h2><p>คุยกับทีม LC TYRE ให้เราช่วยคุณเลือก</p></div><div className="button-row"><a className="primary" href={line}>LINE @lcgroup <ArrowUpRight size={18}/></a><a className="outline" href="tel:0985795449"><Phone size={18}/> 098-579-5449</a></div></section></main>
 <footer><div><a className="logo" href="#"><b>LC<span>TYRE</span><i>↗</i></b></a><p>หลงฉื่อ ขึ้นชื่อเรื่องยาง</p><small>LONG CI GROUP · DESIGN CONCEPT 2026</small></div><div className="footer-links"><a href="#finder">ค้นหายาง</a><a href="#installation">สาขา / จองติดตั้ง</a><a href="#news">ข่าวสาร</a><button onClick={()=>setModal('about')}>เกี่ยวกับเรา</button><button onClick={()=>setModal('status')}>สถานะระบบตัวอย่าง</button></div><div><b>ติดต่อ LC TYRE</b><p><a href="tel:0985795449">098-579-5449</a><br/><a href={line}>LINE @lcgroup</a></p><a className="text-link" href="https://lctyre.com/" target="_blank" rel="noreferrer">เว็บไซต์เดิม ↗</a></div></footer>
 <button className="chat-launcher" aria-expanded={chat} onClick={()=>setChat(!chat)}><MessageCircle size={21}/><span>ถาม LC Tyre AI<small>แชตสาธิต</small></span></button>{chat&&<aside className="chat-panel" aria-label="LC Tyre AI สาธิต"><div className="chat-header"><div><b>LC Tyre AI</b><small>สาธิต · ไม่เชื่อม AI หรือข้อมูลสด</small></div><button className="icon" aria-label="ปิดแชต" onClick={()=>setChat(false)}><X/></button></div><div className="chat-history" role="log" aria-live="polite">{history.map((h,i)=><p key={i} className={h.role}>{h.text}</p>)}</div><div className="quick-actions">{['205/55R16','จองติดตั้ง','ติดต่อเจ้าหน้าที่'].map(t=><button key={t} onClick={()=>send(t)}>{t}</button>)}</div><div className="chat-contact"><a href="tel:0985795449">โทรตอนนี้</a><a href={line}>เปิด LINE ↗</a></div><form className="chat-form" onSubmit={e=>{e.preventDefault();send(message)}}><input aria-label="ข้อความถึง LC Tyre" placeholder="พิมพ์ขนาดยางหรือคำถาม…" value={message} maxLength={500} onChange={e=>setMessage(e.target.value)}/><button aria-label="ส่งข้อความ" type="submit"><Send size={19}/></button></form></aside>}
 <Dialog open={!!detail} onOpenChange={o=>{if(!o)setDetail(null)}}><DialogContent className="detail-dialog"><DialogTitle>รายละเอียดสินค้า</DialogTitle><DialogDescription>ข้อมูลจากหน้าร้าน ณ 7 ก.ย. 2026 กรุณายืนยันราคาและสต็อกก่อนซื้อ</DialogDescription>{detail&&<div className="detail-grid"><img src={detail.image} alt={detail.name}/><div><p className="eyebrow">{detail.brand}</p><h2>{detail.model}</h2><p>{detail.size} · ปี {detail.year}</p><div className="price">฿{money(detail.price)} <small>/ เส้น</small></div><p>สถานะเมื่อดึงข้อมูล: {detail.stock?'มีสินค้า':'หมด'}</p><p className="hint">ตรวจดัชนีรับน้ำหนัก พิกัดความเร็ว และรุ่นย่อยรถก่อนสั่งซื้อ</p><div className="stack"><a className="primary" href={detail.url} target="_blank" rel="noreferrer">ดูสินค้าบนเว็บเดิม <ArrowUpRight size={18}/></a><a className="outline" href={line}>สอบถามผ่าน LINE</a><button className="outline" onClick={()=>{setDetail(null);setModal('booking')}}>ทดลองจองติดตั้ง</button></div></div></div>}</DialogContent></Dialog>
 <Dialog open={article!==null} onOpenChange={o=>{if(!o)setArticle(null)}}><DialogContent className="article-dialog"><DialogTitle>{article!==null?articles[article].title:''}</DialogTitle><DialogDescription>ทีม LC TYRE · ฉบับสาธิตสำหรับตรวจรูปแบบ ยังไม่ใช่บทความเผยแพร่จริง</DialogDescription>{article!==null&&<><img className="article-cover" src={articles[article].image} alt="ยางประกอบบทความ"/><p className="article-text">{articles[article].text}</p><div className="button-row"><button className="outline" onClick={async()=>{try{if(navigator.share){await navigator.share({title:articles[article].title,text:'ตัวอย่างเว็บไซต์ LC TYRE',url:location.href})}else{await navigator.clipboard.writeText(location.href);setShareStatus('คัดลอกลิงก์แล้ว — ลิงก์ localhost เปิดได้บนเครื่องนี้เท่านั้น')}}catch{setShareStatus('ไม่ได้แชร์ลิงก์')}}}>แชร์ / คัดลอกลิงก์</button><button className="primary" onClick={()=>{setArticle(null);document.getElementById('products')?.scrollIntoView()}}>ดูสินค้าแนะนำ</button></div><p role="status" className="hint">{shareStatus}</p></>}</DialogContent></Dialog>
 <Dialog open={!!modal} onOpenChange={o=>{if(!o)setModal('')}}><DialogContent className="general-dialog"><DialogTitle>{({admin:'LC Tyre · ทดลองหลังบ้าน',booking:'เลือกจุดติดตั้ง — สาธิต',account:'บัญชีลูกค้า',cart:'ตะกร้าสินค้า',about:'LONG CI GROUP / LC TYRE',status:'สถานะเว็บตัวอย่าง'} as Record<string,string>)[modal]}</DialogTitle><DialogDescription>ต้นแบบสำหรับตรวจแบบ แยกจากระบบ WordPress และ WooCommerce จริง</DialogDescription>{modal==='admin'?<><Tabs defaultValue="campaign"><TabsList><TabsTrigger value="campaign">แคมเปญ</TabsTrigger><TabsTrigger value="overview">ภาพรวม</TabsTrigger></TabsList><TabsContent value="campaign"><p>ลองแก้ราคาแบนเนอร์สาธิต การเปลี่ยนแปลงมีผลเฉพาะหน้านี้และหายเมื่อโหลดใหม่</p><label className="field">ราคาโปร 4 เส้น<input value={campaignPrice} onChange={e=>setCampaignPrice(e.target.value)} maxLength={12}/></label><button className="primary" onClick={()=>{setSlide(1);setAuto(false);setModal('');document.getElementById('promotion')?.scrollIntoView()}}>ดูแบนเนอร์ที่แก้</button></TabsContent><TabsContent value="overview"><div className="metrics"><div><strong>{products.length}</strong><span>รายการในชุดตัวอย่าง</span></div><div><strong>—</strong><span>ยอดขาย · รอเชื่อม WooCommerce</span></div><div><strong>—</strong><span>ผู้เข้าชม · รอเชื่อม GA4</span></div><div><strong>—</strong><span>คำค้นหา · ยังไม่เก็บข้อมูล</span></div></div><p className="hint">หลังบ้านจริงต้องอยู่หลังระบบล็อกอิน และตรวจสิทธิ์ผู้ใช้ทุกครั้ง</p></TabsContent></Tabs></>:modal==='booking'?<form onSubmit={e=>{e.preventDefault();setBookingMessage(`ตัวอย่างสรุป: ${branch} วันที่ ${bookingDate} ค่าบริการ ฿${money(installationFee+(alignment==='เพิ่มตั้งศูนย์'&&branch!=='สาขาเจริญพัฒนา'?1000:0))} — ยังไม่มีการส่งคำขอ กรุณายืนยันนัดผ่าน LINE`)}}><label className="field">จุดติดตั้ง<Choice value={branch} onChange={v=>{setBranch(v);setAlignment('ไม่เพิ่มบริการ');setBookingMessage('')}} items={['สาขาเจริญพัฒนา','พาร์ตเนอร์ (ตัวอย่าง)']} label="จุดติดตั้ง"/></label>{branch!=='สาขาเจริญพัฒนา'&&<label className="field">บริการเพิ่มเติม<Choice value={alignment} onChange={setAlignment} items={['ไม่เพิ่มบริการ','เพิ่มตั้งศูนย์']} label="บริการเพิ่มเติม"/></label>}<label className="field">วันที่สะดวก<input type="date" required value={bookingDate} min={new Date().toLocaleDateString('en-CA')} onChange={e=>setBookingDate(e.target.value)}/></label><div className="service-row"><span>ค่าบริการตัวอย่าง</span><strong>฿{money(installationFee+(alignment==='เพิ่มตั้งศูนย์'&&branch!=='สาขาเจริญพัฒนา'?1000:0))}</strong></div><p className="hint">ไม่รวมค่ายาง · ไม่ใช่การตรวจเวลาว่างหรือยืนยันนัด</p><button className="primary">ดูสรุปนัดตัวอย่าง</button><p role="status">{bookingMessage}</p><a className="text-link" href={line}>ยืนยันนัดจริงกับเจ้าหน้าที่ผ่าน LINE ↗</a></form>:modal==='status'?<div className="stack"><p>พร้อมทดลอง: หน้าแรก แบนเนอร์ ค้นหาขนาด/แบรนด์ กรอง/เรียงสินค้า รายละเอียดสินค้า บทความ ค่าใช้จ่ายติดตั้ง และแชตตามกฎ</p><p>ยังไม่เชื่อม: WordPress Admin, Orders, Payment, AI Provider, OEM Fitment, GA4 และ Google Reviews</p><p>เว็บจริงไม่ได้แก้ไข · การสำรองฐานข้อมูลและไฟล์เต็มยังรอสิทธิ์โฮสติ้ง</p></div>:modal==='about'?<p>LC TYRE ศูนย์รวมยางรถยนต์ของ LONG CI GROUP<br/>“หลงฉื่อ ขึ้นชื่อเรื่องยาง”<br/>ติดต่อ 098-579-5449 · LINE @lcgroup</p>:<><p>{modal==='cart'?'ตัวอย่างนี้ไม่มีการเพิ่มสินค้า สร้างออร์เดอร์ หรือรับชำระเงิน':'ระบบสมาชิกเดิมยังอยู่บนเว็บไซต์ LC TYRE ไม่ต้องกรอกรหัสผ่านในตัวอย่างนี้'}</p><a className="primary" href="https://lctyre.com/" target="_blank" rel="noreferrer">ไปเว็บไซต์เดิม <ArrowUpRight size={18}/></a></>}</DialogContent></Dialog>
 </>;
}
